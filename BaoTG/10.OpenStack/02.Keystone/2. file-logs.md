# TÌM HIỂU VỀ FILE LOG CỦA SERVICE KEYSTONE
- Do ta cài đặt OpenStack bằng công cụ DevStack nên toàn bộ log của các service đều được quản lý bởi journalctl. Để có thể xem được log của service Keystone, ta sẽ sử dụng câu lệnh
```bash
sudo journalctl -u devstack@keystone.service -f
```
